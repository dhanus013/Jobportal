// Base URL of your backend API.
// Local dev: the backend runs on port 5000, this page is just opened as a file
// or served from a different port — so we always point at localhost:5000 explicitly.
const API_URL = 'http://localhost:5000/api';
